# Launch

Serve the repository root with any simple local web server, then open `index.html` or `app/nexus-main-browser-v10.html`.

Example using Python:

`python -m http.server 8080`

Then visit `http://localhost:8080`.
